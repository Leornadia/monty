#include "monty.h"
/**
 * op_nop - doesn't do anything
 * @stack: stack (unused) 
 * @line_number: current instruction line number (unused)
*/
void op_nop(stack_t **stack, unsigned int line_number)
{
(void) stack;
(void) line_number;
}
